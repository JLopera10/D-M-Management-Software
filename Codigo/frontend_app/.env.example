# Copia este archivo a .env y ajusta.
# Cómo obtener la URL correcta del iframe: abre Google Maps → busca el lugar → Compartir → Insertar un mapa → copia solo el valor de src="..."
# El enlace corto (maps.app.goo.gl) no es válido como VITE_GOOGLE_MAPS_EMBED_URL.
VITE_GOOGLE_MAPS_EMBED_URL=
